language: python
python:
 - "3.4"

#command to install dependencies
install:
 - "pip install -r requirements.txt"
 - "pip install -U selenium"

#command to run tests
script: "chown 777 ../database/ ; python3 manage.py runserver; python3 manage.py test"
